program calculation(input, output);
var l : integer;

begin
    writeln('Please enter the length of a side ...');
    readln(l);
    writeln('The volume is ', l * l * l);
    writeln('The total surface area is ', l * l * 6);
    readln;
end.